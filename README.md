# file-processing-system
A frontend application for a project-centric file processing system using React, which includes APIs integration, authentication support, project management, file uploads, and background job tracking.

## Tech Stack
- React
- TypeScript
- Vite
- Json server(mock backend)

## Setup
- Forntend runs on vite dev server
- Backend runs on port 4000 (json-server)